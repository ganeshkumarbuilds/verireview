<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Phase 1 skeleton: minimal deps (webmvc + webmvc-test only). Security, JPA,
       Flyway, MapStruct, OpenAPI arrive in their roadmap phases.
       NOTE (Spring Boot 4.x): `spring-boot-starter-web` is a deprecated alias
       of `spring-boot-starter-webmvc`; test slices live in the per-technology
       `-test` starter, which transitively brings `spring-boot-starter-test`. -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.verireview</groupId>
  <artifactId>verireview-backend</artifactId>
  <version>0.1.0</version>
  <name>VeriReview Backend</name>
  <description>VeriReview Spring Boot modular monolith (Phase 1 skeleton).</description>

  <properties>
    <java.version>21</java.version>
    <!-- Aligned with spring-boot-testcontainers 4.1.1 (see its pom). -->
    <testcontainers.version>2.0.5</testcontainers.version>
    <!-- Single JWT library for Phase 3 (SECURITY_DESIGN §2, ADR-009). -->
    <jjwt.version>0.12.6</jjwt.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>

    <!-- Phase 2: persistence (JPA/Hibernate, PostgreSQL, Flyway). -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Phase 3: security + auth (BCrypt, JWT HS256, Bean Validation, JSON DTOs).
         Still excluded: MapStruct, OpenAPI, Redis, Kafka, AI. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-json</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Phase 2: real PostgreSQL in tests via Testcontainers. No H2.
         Single-container singleton wired with @DynamicPropertySource, so no
         Boot @ServiceConnection glue or JUnit extension is needed. -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-postgresql</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <!-- PostgreSQL rejects legacy JVM timezone aliases (e.g. Asia/Calcutta)
               as a connection TimeZone; pin UTC like the test JVMs (see README). -->
          <jvmArguments>-Duser.timezone=UTC</jvmArguments>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- Deterministic timezone: PostgreSQL rejects legacy JVM aliases
               such as Asia/Calcutta as a TimeZone startup parameter. -->
          <argLine>-Duser.timezone=UTC</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
