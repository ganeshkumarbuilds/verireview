"""VeriReview AI service (Phase 7A).

Proposes structured agent results for the Spring Boot backend. This package
MUST NOT touch PostgreSQL, modify project files, or execute uploaded code
(AGENTS.md rules 7/14, ADR-005). It receives scoped data and returns
versioned JSON; the backend applies policy and persists state.
"""

__version__ = "0.1.0"
